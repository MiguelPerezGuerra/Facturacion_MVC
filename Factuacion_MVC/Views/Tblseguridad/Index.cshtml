@model IEnumerable<Factuacion_MVC.Models.Tblseguridad>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StrUsuario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StrClave)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DtmFechaModifica)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StrUsuarioModifico)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdEmpleadoNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StrUsuario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StrClave)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DtmFechaModifica)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StrUsuarioModifico)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdEmpleadoNavigation.IdEmpleado)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdSeguridad">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdSeguridad">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdSeguridad">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
