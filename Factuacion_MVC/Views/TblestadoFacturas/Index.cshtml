@model IEnumerable<Factuacion_MVC.Models.TblestadoFactura>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StrDescripcion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StrDescripcion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdEstadoFactura">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdEstadoFactura">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdEstadoFactura">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
